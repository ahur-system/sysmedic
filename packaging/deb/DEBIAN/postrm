#!/bin/bash
set -e

# postrm script for sysmedic
# see: dh_installdeb(1)

case "$1" in
    purge)
        # Remove configuration files and data
        rm -rf /etc/sysmedic
        rm -rf /var/lib/sysmedic

        # Remove systemd service files
        rm -f /etc/systemd/system/sysmedic.doctor.service
        rm -f /etc/systemd/system/sysmedic.websocket.service

        # Reload systemd
        systemctl daemon-reload || true

        echo "SysMedic has been completely removed from the system."
        ;;

    remove|upgrade|failed-upgrade|abort-install|abort-upgrade|disappear)
        # Reload systemd daemon to clean up
        systemctl daemon-reload || true
        ;;

    *)
        echo "postrm called with unknown argument \`$1'" >&2
        exit 1
        ;;
esac

exit 0
